import { mySqrt } from './mySqrt';

describe('69. Sqrt(x)', () => {
    test('Example 1', () => {
        expect(mySqrt(4)).toBe(2);
    });

    test('Example 2', () => {
        expect(mySqrt(8)).toBe(2);
    });
});
