// import { firstBadVersion } from './isBadVersion';


describe('278. First Bad Version', () => {
    test('Example 1', () => {
        // expect(firstBadVersion(4)(5)).toBe(4);
    });

    test('Example 2', () => {
        // expect(firstBadVersion(1)(1)).toBe(1);
    });
});
